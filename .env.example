# CiRA ME - Environment Configuration Example
# Copy this file to .env and adjust values as needed

# =============================================================================
# Flask Backend Configuration
# =============================================================================

# Enable debug mode (set to False in production)
FLASK_DEBUG=False

# =============================================================================
# Frontend Configuration
# =============================================================================

# Port to expose the frontend application
FRONTEND_PORT=3030

# =============================================================================
# Data Persistence Paths
# =============================================================================
# These paths define where Docker volumes will be mounted on the host machine
# Ensure these directories exist or will be created with proper permissions

# Path for application database
CIRAME_DATA_PATH=./docker-volumes/data

# Path for trained ML models
CIRAME_MODELS_PATH=./docker-volumes/models

# Path for datasets
CIRAME_DATASETS_PATH=./docker-volumes/datasets

# =============================================================================
# Docker Compose Configuration
# =============================================================================

# Compose project name (optional - defaults to directory name)
COMPOSE_PROJECT_NAME=cirame

# =============================================================================
# Production Settings
# =============================================================================

# For production deployments, consider:
# - Using absolute paths for volume mounts
# - Implementing backup strategies for data/models
# - Setting up log rotation
# - Configuring reverse proxy (nginx/traefik) for SSL/TLS
# - Implementing monitoring and alerting

# Example production paths (Linux):
# CIRAME_DATA_PATH=/var/lib/cirame/data
# CIRAME_MODELS_PATH=/var/lib/cirame/models
# CIRAME_DATASETS_PATH=/var/lib/cirame/datasets

# Example production paths (Windows):
# CIRAME_DATA_PATH=C:/CiRAME/data
# CIRAME_MODELS_PATH=C:/CiRAME/models
# CIRAME_DATASETS_PATH=C:/CiRAME/datasets
