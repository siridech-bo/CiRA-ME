# CiRA ME - Backend Dependencies
# Machine Intelligence for Edge Computing

# Core Flask — pin werkzeug<3.1 to avoid breaking change on Python 3.11
Flask==3.0.3
Flask-CORS>=4.0.0,<5.0
Werkzeug>=3.0.0,<3.1

# Database
# SQLite is built-in to Python

# Data Processing — pin pandas<2.2.3 to avoid import breakage
pandas>=2.0.0,<2.2.3
numpy>=1.24.0,<2.0
scipy>=1.14.0

# Machine Learning - Classification
scikit-learn>=1.3.0

# Machine Learning - Anomaly Detection
pyod>=1.1.0

# Machine Learning - Regression (Gradient Boosting)
xgboost>=2.0.0
lightgbm>=4.0.0

# Deep Learning - TimesNet
# torch is installed separately in the Dockerfile from the correct
# cuXXX --index-url based on build args (see Dockerfile ARG TORCH_*).
# The range here loosely brackets the two supported versions
# (2.0.1 for cu117, 2.10.0 for cu128) so `pip install -r` does not
# fight with the version already installed.
torch>=2.0.1

# Feature Extraction
tsfresh>=0.20.0

# CBOR Support — pin to 5.x (pure Python) to avoid Rust PanicException
# in 6.x that crashes on Edge Impulse signed CBOR files (uncatchable by `except Exception`)
cbor2>=5.5.0,<6.0

# SSH Deployment
paramiko>=3.3.0
scp>=0.14.0

# Model Export
joblib>=1.3.0
skl2onnx>=1.16
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1

# HTTP Client (for TI ModelMaker bridge)
requests>=2.31.0

# System Monitoring
psutil>=5.9.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Development
python-dotenv>=1.0.0

# MQTT client for test publisher
paho-mqtt>=2.0.0
